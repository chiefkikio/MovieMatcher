<%= form_for @movie, :html => {:class => "form-group"} do |f| %>
  <% if @movie.errors.any? %>
    <div id="error_explanation">
      <div class="alert alert-danger" role="alert">
      <h4><span class="glyphicon glyphicon-warning-sign"></span>   <%= pluralize(@movie.errors.count, "error") %> prohibited this movie from being saved:</h4>
      <ul>
      <% @movie.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


    <%= f.label :title, "What movie was he assigned to photograph again?:", class: "control-label" %><br>
    <%= f.text_field :title,  class: "form-control", :placeholder => "ie. The Fast and the Fur-ious, The Purrincess Bride, etc... " %>
    <%= f.submit "Let's Play", class: "btn btn-default center-block" %>  

<% end %>
